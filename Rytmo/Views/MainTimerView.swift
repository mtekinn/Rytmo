import SwiftUI

struct MainTimerView: View {
    var body: some View {
        VStack {
            Text("Timer Display") // placeholder for now
            Button("Start Routine") {
                // Will implement functionality later
            }
        }
    }
}
